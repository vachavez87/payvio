export const VIRTUALIZATION = {
  ROW_HEIGHT: 65,
} as const;

export const ANIMATION = {
  FAST: 150,
  NORMAL: 300,
  SLOW: 500,
  STAGGER: 50,
  MICRO: 120,
  BUTTON_PRESS_SCALE: 0.98,
} as const;

export const CHART = {
  HEIGHT: 320,
  PANEL_HEIGHT: 400,
  BAR_RADIUS: [4, 4, 0, 0] as [number, number, number, number],
  TOOLTIP_BORDER_RADIUS: 8,
  GRID_DASH: "3 3",
  TOOLTIP_SHADOW: "0 4px 12px rgba(0,0,0,0.1)",
} as const;

export const UI = {
  ALPHA_HOVER: 0.04,
  ALPHA_LIGHT: 0.02,
  ALPHA_MEDIUM: 0.08,
  ALPHA_MUTED: 0.1,
  ALPHA_ACTIVE: 0.12,
  ALPHA_FOCUS_RING: 0.2,
  ALPHA_BORDER: 0.2,
  ALPHA_BORDER_DARK: 0.16,
  ALPHA_OVERLAY: 0.5,
  SIDEBAR_WIDTH: 260,
  SIDEBAR_COLLAPSED_WIDTH: 72,
  TOP_BAR_HEIGHT: 56,
  DRAWER_WIDTH: 280,
  MENU_MIN_WIDTH: 180,
  HEADER_HEIGHT: 80,
  HEADER_TOTAL_HEIGHT: 112,
  TOAST_MIN_WIDTH: 300,
  TOAST_DURATION_DEFAULT: 5000,
  TOAST_DURATION_LONG: 8000,
  TOAST_OFFSET: 80,
  MAX_VISIBLE_TOASTS: 5,
  PAGE_TRANSITION_DURATION: 400,
  PAGE_TRANSITION_OFFSET: 16,
  PROGRESS_BAR_HEIGHT: 6,
  TOTALS_MIN_WIDTH: 280,
  EMPTY_STATE_ICON_SIZE: 64,
  EMPTY_STATE_ICON_FONT_SIZE: 40,
  EMPTY_STATE_SEARCH_ICON_SIZE: 40,
  METRIC_ICON_SIZE: 44,
  NAV_PILL_HEIGHT: 36,
  SIDEBAR_ICON_SIZE: 20,
  AVATAR_SIZE: 32,
  STATUS_DOT_SIZE: 8,
  TIMELINE_DOT_SIZE: 32,
  TIMELINE_LINE_WIDTH: 2,
  TIMELINE_LINE_LEFT: 15,
  TIMELINE_LINE_TOP: 32,
  HEADER_BACKDROP_BLUR: 12,
  HEADER_BG_ALPHA: 0.85,
  LOGO_SHADOW_ALPHA: 0.3,
  BRANDING_LOGO_MAX_WIDTH: 200,
  BRANDING_LOGO_MAX_HEIGHT: 60,
  BRANDING_PREVIEW_MAX_WIDTH: 120,
  BRANDING_PREVIEW_MAX_HEIGHT: 40,
  COMMAND_PALETTE_MAX_HEIGHT: 400,
  FILTER_DRAWER_MAX_HEIGHT: "80vh",
  FILTER_DRAWER_HANDLE_WIDTH: 40,
  FILTER_DRAWER_HANDLE_HEIGHT: 4,
  FOCUS_RING_WIDTH: 3,
  BORDER_RADIUS_SM: 8,
  BORDER_RADIUS_MD: 12,
  BORDER_RADIUS_LG: 16,
  BORDER_RADIUS_XL: 20,
  ICON_SIZE_XS: 16,
  ICON_SIZE_SM: 18,
  ICON_SIZE_MD: 28,
  ICON_SIZE_XL: 48,
  ICON_BADGE_SIZE: 36,
  DRAG_HANDLE_HEIGHT: 40,
  DRAG_OPACITY: 0.5,
  TABLE_ACTION_WIDTH: 48,
  LOADER_SIZE_SM: 16,
  LOADER_SIZE_MD: 20,
  LOADER_SIZE_LG: 40,
  SKELETON_AVATAR_SIZE: 48,
  COLOR_PICKER_SIZE: 48,
  FIELD_WIDTH_QTY: 90,
  FIELD_WIDTH_PRICE: 130,
  FIELD_WIDTH_RATE: 120,
  FIELD_WIDTH_REMINDER: 100,
  FONT_SIZE_NAV: "0.9375rem",
  LANDING_MAX_WIDTH: 1200,
  FAVICON_SIZE: 128,
  FAVICON_ICON_SIZE: 84,
} as const;

export const RESPONSIVE_SX = {
  DESKTOP_ONLY: { display: { xs: "none", sm: "flex" } },
  DESKTOP_MD_ONLY: { display: { xs: "none", md: "flex" } },
  MOBILE_ONLY: { display: { xs: "flex", sm: "none" } },
} as const;
